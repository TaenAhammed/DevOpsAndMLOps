apiVersion: v1
kind: Service
metadata:
  name: {{ include "genzfashion-api.fullname" . }}
  labels:
    {{- include "genzfashion-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "genzfashion-api.selectorLabels" . | nindent 4 }}
